pipeline {
   agent any

   stages {
      stage('git clone') {
         steps {
            sh 'echo Downloading code'
         }
      }
   }
   
   stage('code compile') {
         steps {
            sh 'echo compiling the code'
         }
      }
   }
}
